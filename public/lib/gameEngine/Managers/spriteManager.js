define([], function() {

  function SpriteManager(image, dict) {
    this.image = image;
    this.dic = dict;
  };

  SpriteManager.prototype.loadSprites = function (dict) {

  };

  SpriteManager.prototype.ge
  return SpriteManager;
});
